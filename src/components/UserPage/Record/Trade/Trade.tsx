import { TradeProps } from "../../../../utils/interface";
import * as S from "./Trade.style";

const Trade = ({ accessId }: TradeProps) => {
  return <S.TradeContainer>Trade</S.TradeContainer>;
};

export default Trade;
